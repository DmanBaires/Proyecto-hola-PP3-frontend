
# Hola Mundo - Frontend (estático)

Abre `index.html` localmente o deployalo como sitio estático (Netlify/Vercel).
La página permite probar:
- Backend: GET /api/hola
- Servicio X: GET /api/frase
- Recurso REST usuarios
